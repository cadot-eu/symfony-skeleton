version: '3'
services:
  {DIR}:
    image: cadotinfo/symfony5
    container_name: {DIR}
    volumes:
      - .:/app
    networks:
      - web
    restart: always
    ports:
      - 80


networks:
  web:
    external: true
